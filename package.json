{
  "name": "vitest-browser-examples",
  "type": "module",
  "version": "0.0.0",
  "description": "Examples of using Vitest Browser Mode with different frameworks",
  "keywords": [],
  "license": "MIT",
  "author": "Vladimir Sheremet",
  "scripts": {
    "test": "pnpm -r --filter !marko --sequential test -- --no-watch --browser.headless"
  },
  "devDependencies": {
    "playwright": "^1.44.1",
    "typescript": "^5.5.2"
  },
  "pnpm": {
    "overrides": {
      "vitest": "^2.1.0-beta.4",
      "@vitest/browser": "^2.1.0-beta.4"
    }
  }
}
