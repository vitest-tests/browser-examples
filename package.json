{
  "name": "vitest-browser-examples",
  "type": "module",
  "version": "0.0.0",
  "description": "Examples of using Vitest Browser Mode with different frameworks",
  "keywords": [],
  "author": "Vladimir Sheremet",
  "scripts": {
    "test": "pnpm -r --sequential test -- --no-watch --browser.headless"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "vitest": "^1.6.0"
  },
  "pnpm": {
    "overrides": {
      "vitest": "link:../vitest/packages/vitest",
      "@vitest/browser": "link:../vitest/packages/browser"
    }
  }
}
